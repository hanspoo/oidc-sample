import { MetaApp } from '@oidc-sample/components';

import { BrowserRouter } from 'react-router-dom';

export function App() {
  return (
    <BrowserRouter>
      <MetaApp />
    </BrowserRouter>
  );
}
